<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>织星</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://misterrabbit0w0.github.io/</id>
  <link href="https://misterrabbit0w0.github.io/" rel="alternate"/>
  <link href="https://misterrabbit0w0.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, 织星</rights>
  <subtitle>织星的折腾笔记</subtitle>
  <title>织星 · Astraea</title>
  <updated>2026-06-13T13:08:50.387Z</updated>
  <entry>
    <author>
      <name>织星</name>
    </author>
    <category term="AI" scheme="https://misterrabbit0w0.github.io/categories/AI/"/>
    <category term="Agent" scheme="https://misterrabbit0w0.github.io/tags/Agent/"/>
    <category term="Prompt Engineering" scheme="https://misterrabbit0w0.github.io/tags/Prompt-Engineering/"/>
    <content>
      <![CDATA[<p>本文相关信息来源截止至 2026 年 6 月，后续可能会有变化，请以最新信息为准。</p><hr><p>这篇文章属于半扫盲性质的 agent 使用说明，小白也可放心食用~</p><hr><p>在聊如何使用之前，我们先来了解一些基础概念。</p><h2 id="Agent-是什么？"><a href="#Agent-是什么？" class="headerlink" title="Agent 是什么？"></a>Agent 是什么？</h2><p>先来认识几个名词</p><blockquote><p>agent n. 代理人；代理商；经纪人<br>model n. 模型；模特儿；模范<br>harness v. 利用；驾驭；控制</p></blockquote><p>所以可以简单地认为，agent 就是被系统驾驭的模型，能产出什么内容取决于两个方向，一是模型本身的能力，二是系统提供的能力。就好比电脑不但要有硬件，还要有操作系统和软件，才能发挥作用。</p><hr><p>大部分人说的 ai 其实并没有区分 <em>model</em> 和 <em>agent</em>，举个例子，大部分人用的豆包，其实是一个 <em>agent</em>（可能能力较弱），它背后的模型是 <code>doubao-seed-2.0(-pro)</code>，在豆包这个软件中，模型拥有了更多的能力（比如记忆、工具调用等），所以能产出更多的内容。</p><hr><p>目前常见的国外模型有：<code>claude, gemini, gpt</code>，有的可能还带有版本号，例如 <code>claude-opus-4-8</code>, <code>gpt-5.5-thinking</code>，<code>gemini-3.5-flash</code>（注意 anthropic 家的版本号用连字符而不是点）。<br>命名一般会暗示模型的强度，例如 flash 版本弱于 pro，版本号越新模型一般更强（但是有的厂商会开倒车）。当版本号和档位都不同时就不太好比，例如 <code>gemini-3.1-pro</code> 和 <code>gemini-3.5-flash</code>（我个人认为前者更强）。<br>国内的相对来说种类更多，主流的有：<code>deepseek</code>, <code>glm</code>, <code>kimi</code>, <code>qwen</code> 和 <code>doubao</code>，相关的版本规范也和国外的差不多。</p><p>还有一些额外的后缀，类似 <code>-thinking</code>, <code>-it</code>，<code>-xxb-axb</code>, 等等，这里不做讲解，感兴趣的可以自行搜索。</p><hr><p>再来谈谈有关的工具，也就是 harness，也可以称之为鞍，毕竟好马配好鞍。<br>目前最火的 agent 工具应该是 anthropic 的 <code>claude code</code>，简称 <code>cc</code>，它本身只原生支持 claude 系列模型，不过现在有工具能方便地接入其他模型了。<br>还有类似 codex，<a href="https://github.com/anomalyco/opencode">opencode</a>，<a href="https://github.com/earendil-works/pi">pi</a> 等一系列工具，这里就不一一举例。<br>harness 为 skills 提供了接口，skills 则拓展了 AI 的能力，例如写 word, ppt 等文档，调用浏览器或其他软件的能力。</p><h2 id="如何使用-agent？"><a href="#如何使用-agent？" class="headerlink" title="如何使用 agent？"></a>如何使用 agent？</h2><p>这一部分我会分为三节：</p><ul><li>如何安装 agent 工具 （安装 harness）</li><li>如何接入模型</li><li>如何拓展 agent 的能力（安装 skills）</li></ul><h3 id="如何安装-agent"><a href="#如何安装-agent" class="headerlink" title="如何安装 agent"></a>如何安装 agent</h3><p>这里以 <code>claude code</code> 为例。</p><h4 id="自动安装方法（推荐）"><a href="#自动安装方法（推荐）" class="headerlink" title="自动安装方法（推荐）"></a>自动安装方法（推荐）</h4><p>打开任意一个你能连接国外网络的浏览器，搜索 <code>claude code</code>，进入官网，目前官网链接是 <a href="https://claude.com/product/claude-code">这个</a>。官网的样子应该长成这样：</p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/claude-official.png" alt="Claude Code 官网首页"></p><p>看到页面中间的命令了吗？把它复制下来，用你电脑上的终端（terminal），粘贴这行命令，回车，就能安装了！如果你是 Windows 用户，建议使用 PowerShell 终端，Mac 用户可以使用自带的 Terminal 终端。</p><p>Windows（PowerShell）：</p><pre><code class="highlight powershell"><span class="built_in">irm</span> https://claude.ai/install.ps1 | <span class="built_in">iex</span></code></pre><p>macOS &#x2F; Linux：</p><pre><code class="highlight bash">curl -fsSL https://claude.ai/install.sh | bash</code></pre><p><strong>注意！</strong> 永远不要运行你不知道来源的命令，部分命令可能参杂有害代码，对电脑和财产造成危害，所以一定要确认命令的来源和内容。</p><p>这行命令是直接从官网复制的，可能会随着时间变化而变化，如果你发现官网上的命令和这里不一样，请以官网上的为准。</p><p>等待时间可能有点久，可以给终端设置代理加速，设置代理命令为</p><p>PowerShell：</p><pre><code class="highlight powershell"><span class="variable">$env:HTTP_PROXY</span>=<span class="string">&quot;your_proxy_url&quot;</span>; <span class="variable">$env:HTTPS_PROXY</span>=<span class="string">&quot;your_proxy_url&quot;</span></code></pre><p>cmd：</p><pre><code class="highlight bat"><span class="built_in">set</span> http_proxy=your_proxy_url<span class="built_in">set</span> https_proxy=your_proxy_url</code></pre><p>上述命令只在当前终端会话中生效，如果关闭终端后再打开，就需要重新设置一次。</p><h4 id="手动安装方法"><a href="#手动安装方法" class="headerlink" title="手动安装方法"></a>手动安装方法</h4><p>如果自动安装方法遇到了问题，可以使用以下手动安装方法。</p><p>首先，安装 Node.js，官网链接是 <a href="https://nodejs.org/">这个</a>，下载并安装最新的 LTS 版本。</p><p>安装完成后，打开终端，先验证 Node.js 是否安装成功，输入以下命令：</p><pre><code class="highlight bash">node -v</code></pre><p>如果显示了版本号，说明 Node.js 安装成功了。接下来，安装 <code>claude code</code>，输入以下命令：</p><pre><code class="highlight bash">npm install -g @anthropic-ai/claude-code@latest</code></pre><p>这一步需要的时间可能也比较久（问就是 nodejs 太臃肿），安装完成后，输入以下命令验证是否安装成功：</p><pre><code class="highlight bash">claude --version</code></pre><p>如果显示了版本号，说明 <code>claude code</code> 安装成功了。</p><hr><p>接下来要说明两个概念：<strong>用户级</strong> 和 <strong>项目级</strong>。</p><p>用户级（User-level）是指在本电脑用户范围内会生效的设置，工具或技能，此用户的所有项目都可以访问它们。用户级的配置位于 <code>~/.claude</code>（Windows 为 <code>%USERPROFILE%\.claude</code>） 目录下。</p><p>项目级（Project-level）是指在特定项目范围内会生效的设置，工具或技能，只有这个项目能访问它们。项目级的配置位于项目根目录下的 <code>.claude</code> 目录中。</p><h3 id="如何接入模型"><a href="#如何接入模型" class="headerlink" title="如何接入模型"></a>如何接入模型</h3><p>目前，国内想要用到完整的 claude&#x2F;gpt&#x2F;gemini 模型相对困难，这里给出用 deepseek 官方模型的示例，其他模型的接入方法大同小异。</p><p><strong>本教程不会对任何所谓 <em>中转站</em> 做出推荐和评价，也不会为任何因使用中转站而导致的问题负责，请自行判断和选择。</strong></p><h4 id="安装-cc-switch-（推荐）"><a href="#安装-cc-switch-（推荐）" class="headerlink" title="安装 cc-switch （推荐）"></a>安装 cc-switch （推荐）</h4><p>cc-switch 相当于一个懒人包，可以方便接入各种模型。可自行前往 <a href="https://github.com/farion1231/cc-switch/releases/latest">Github Release</a> 页面下载对应版本并安装。</p><p>首先，打开 deepseek 的官网，注册并登录你的账号。进入 <a href="https://platform.deepseek.com/">API 开放平台</a>。</p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/deepseek-official.png" alt="DeepSeek API 开放平台"></p><p>所有 agent 工具的使用都需要用到 API key，这个和网页端使用不一样，不是免费的！（可能有免费渠道，但请自己甄别）你必须先给模型厂商充值，才能使用 API key 调用他们的模型。一般 deepseek 充值 10 块左右就能用很久（按照用途不同，使用时间也不同）。</p><p>然后在 <a href="https://platform.deepseek.com/api_keys">API keys</a> 页面，选择创建 API key，输入名字（比如 claude-code），点击创建，就能看到你的 API key 了。</p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/api-key.png" alt="创建 DeepSeek API key"></p><p><strong>注意！</strong> deepseek 的 API key 只在创建时可见，一定要妥善保管。它是非常敏感的信息——拿到它的人就能用你的余额调用模型，和银行卡密码一个性质，千万不要泄露给任何人，包括我！不管是弄丢了还是不小心泄露了，都请立即删除这个 key 并重新创建。</p><p>如果你下载了 cc-switch，打开软件后点击右上角加号，在预设供应商中选择 <code>DeepSeek</code>，一般除了 API key 以外的部分不需要修改，只要把你的 API key 填入对应位置即可，如果其他地方的设置有问题，可以按照我图片中的内容填。</p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/cc-switch-official.png" alt="cc-switch"></p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/cc-switch-2-official.png" alt="cc-switch"></p><p>如果没有下载此工具，有以下两种方式接入模型：</p><h4 id="临时配置"><a href="#临时配置" class="headerlink" title="临时配置"></a>临时配置</h4><p>在终端中输入以下命令</p><p>Linux &#x2F; macOS：</p><pre><code class="highlight bash"><span class="built_in">export</span> ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic<span class="built_in">export</span> ANTHROPIC_AUTH_TOKEN=&lt;你的 DeepSeek API Key&gt;<span class="built_in">export</span> ANTHROPIC_MODEL=deepseek-v4-pro[1m]<span class="built_in">export</span> ANTHROPIC_DEFAULT_OPUS_MODEL=deepseek-v4-pro[1m]<span class="built_in">export</span> ANTHROPIC_DEFAULT_SONNET_MODEL=deepseek-v4-pro[1m]<span class="built_in">export</span> ANTHROPIC_DEFAULT_HAIKU_MODEL=deepseek-v4-flash<span class="built_in">export</span> CLAUDE_CODE_SUBAGENT_MODEL=deepseek-v4-flash<span class="built_in">export</span> CLAUDE_CODE_EFFORT_LEVEL=max</code></pre><p>Windows（PowerShell）：</p><pre><code class="highlight powershell"><span class="variable">$env:ANTHROPIC_BASE_URL</span>=<span class="string">&quot;https://api.deepseek.com/anthropic&quot;</span><span class="variable">$env:ANTHROPIC_AUTH_TOKEN</span>=<span class="string">&quot;&lt;你的 DeepSeek API Key&gt;&quot;</span><span class="variable">$env:ANTHROPIC_MODEL</span>=<span class="string">&quot;deepseek-v4-pro[1m]&quot;</span><span class="variable">$env:ANTHROPIC_DEFAULT_OPUS_MODEL</span>=<span class="string">&quot;deepseek-v4-pro[1m]&quot;</span><span class="variable">$env:ANTHROPIC_DEFAULT_SONNET_MODEL</span>=<span class="string">&quot;deepseek-v4-pro[1m]&quot;</span><span class="variable">$env:ANTHROPIC_DEFAULT_HAIKU_MODEL</span>=<span class="string">&quot;deepseek-v4-flash&quot;</span><span class="variable">$env:CLAUDE_CODE_SUBAGENT_MODEL</span>=<span class="string">&quot;deepseek-v4-flash&quot;</span><span class="variable">$env:CLAUDE_CODE_EFFORT_LEVEL</span>=<span class="string">&quot;max&quot;</span></code></pre><p><strong>注意！</strong> 这种方式只在当前终端会话中生效，如果关闭终端后再打开，就需要重新设置一次。</p><h4 id="永久配置"><a href="#永久配置" class="headerlink" title="永久配置"></a>永久配置</h4><p>更推荐直接写入 Claude Code 的配置文件 <code>~/.claude/settings.json</code>（Windows 为<br>  <code>%USERPROFILE%\.claude\settings.json</code>）。在文件中添加以下内容：</p><pre><code class="highlight json"><span class="punctuation">&#123;</span>  <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span>    <span class="attr">&quot;ANTHROPIC_BASE_URL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;https://api.deepseek.com/anthropic&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;ANTHROPIC_AUTH_TOKEN&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&lt;你的 DeepSeek API Key&gt;&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;ANTHROPIC_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;deepseek-v4-pro[1m]&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;ANTHROPIC_DEFAULT_OPUS_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;deepseek-v4-pro[1m]&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;ANTHROPIC_DEFAULT_SONNET_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;deepseek-v4-pro[1m]&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;ANTHROPIC_DEFAULT_HAIKU_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;deepseek-v4-flash&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;CLAUDE_CODE_SUBAGENT_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;deepseek-v4-flash&quot;</span><span class="punctuation">,</span>    <span class="attr">&quot;CLAUDE_CODE_EFFORT_LEVEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;max&quot;</span>  <span class="punctuation">&#125;</span><span class="punctuation">&#125;</span></code></pre><h3 id="如何拓展-agent-的能力"><a href="#如何拓展-agent-的能力" class="headerlink" title="如何拓展 agent 的能力"></a>如何拓展 agent 的能力</h3><p>Agent 的能力拓展主要是通过安装 skills 来实现的，skills 就好比是 agent 的插件，可以让 agent 拥有更多的功能，例如写文档、调用浏览器等。</p><p>skills 分用户级和项目级（和前面说的配置分级一致），具体把一个 skill 安装在哪一级取决于其功能范围。</p><p>用户级 skill 的安装位置是：<code>~/.claude/skills</code>。<br>项目级 skill 的安装位置是：项目根目录下的 <code>.claude/skills</code>。</p><p>目前，claude code 已经内置了很多实用的 skills，这些 skills 可以直接使用，无需额外安装。</p><p>如果你想要安装第三方的 skills，可以通过以下方式进行安装：</p><h4 id="利用-SKillpkg-安装和管理"><a href="#利用-SKillpkg-安装和管理" class="headerlink" title="利用 SKillpkg 安装和管理"></a>利用 SKillpkg 安装和管理</h4><p>这里推荐一个朋友写的 <a href="https://github.com/Richardlxr/SKillpkg">skill 管理工具</a>，可以按照手册进行管理和安装。</p><h4 id="手动安装"><a href="#手动安装" class="headerlink" title="手动安装"></a>手动安装</h4><p><del>其实可以让你已经安装的 agent 帮你下载和管理 skills</del></p><p>一般的 skills，以 <a href="https://github.com/obra/superpowers">superpowers</a> 为例，会提供完整的安装说明。</p><p>如果没有提供安装说明，一般来说，下载后把 skills 文件夹放到 <code>~/.claude/skills</code> 目录下就可以了。</p><p>第一次安装 skills 可能没有这个文件夹，自行创建即可。</p><p>一个 skills 的文件结构一般是这样的：</p><pre><code class="highlight text">技能名字/├── SKILL.md // 技能入口，一般 agent 会尝试发现此文件来预加载 skills├── requirements.txt // 有的技能可能涉及运行 python 脚本，这个文件会列出所需的 python 包├── references/ // 参考资料，可能包含一些文档、图片等└── scripts/ // 可能包含一些脚本文件，例如 python 脚本等</code></pre><p>安装完成后，可以在 agent 中调用 <code>/skills</code> 命令查看 skills 的安装状态。一般通过 <code>/技能名字</code> 就可以调用技能。</p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/skill-list.png" alt="skill-list"><br><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/use-a-skill.png" alt="use-a-skill"></p><p><strong>注意！</strong> 在安装第三方 skills 时，一定要确认 skills 的来源和内容，尤其是那些包含脚本文件的 skills，因为它们可能会执行一些你不希望执行的操作，所以一定要谨慎选择和安装 skills。</p><h2 id="如何让-agent-帮你干活"><a href="#如何让-agent-帮你干活" class="headerlink" title="如何让 agent 帮你干活"></a>如何让 agent 帮你干活</h2><p>很多人提到 claude code，会误认为只能用来写代码，但是因为其能阅读本地文件的特性，所以也可以用来处理一些日常 workflow。</p><p>下面以大家 <del>喜闻乐见</del> 的汇报PPT为例，讲解如何用 skill: <a href="https://github.com/hugohe3/ppt-master">ppt-master</a> 生成PPT。</p><p>（非必需项）如果你的 PPT 刚需视觉元素，建议选择一个 <strong>多模态</strong> 模型接入 claude code，例如 kimi-k2.6&#x2F;minimax-m3，deepseek 所有模型能上传图片的本质是通过 OCR 提取图片中的文字进行分析。</p><p>首先，新建一个文件夹作为 claude code 工作区，claude code 必须运行在工作区里。</p><p>我一般会使用 <code>claude --dangerously-skip-permissions</code> 指令打开 claude code，这串东西的意思是自动批准所有请求，直接运行。所以如果你对模型能力保持怀疑，或你不确定你的模型来源，请不要使用此功能，并务必核对每条操作指令。</p><p>然后，安装 <a href="https://github.com/hugohe3/ppt-master">ppt-master</a>，官网有不同系统的安装提示，如果你不确定，直接让 claude code 为你安装即可。</p><p><img src="/2026/06/02/2026-06-02-elegant-agent-guideline/install-ppt-master.png" alt="install-ppt-master"></p><p>然后把参考的模板和资料丢到工作文件夹中，使用类似以下的提示词。</p><pre><code class="highlight text">/ppt-master 生成用于 [你的使用场景] 的演示文稿，参考 @[如果你有模板，此处填模板路径] 的格式，参考资源 @[你要讲的相关文件路径]</code></pre><p>一般模型可能会再问你几个相关设计问题，回答之后就会开始生成 PPT。</p><p>如果一切顺利，AI会告诉你演示文稿的路径，审阅过后可以提出修改意见（例如全局换字体，某一页的排版更换等）</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这是一份简易的关于如何安装和使用 agent 的指南，主要以 <code>claude code</code> 为例进行说明，其他 agent 工具的使用方法大同小异。如果有帮助，可以<a href="https://github.com/MisterRabbit0w0/misterrabbit0w0.github.io">点一个 star</a> 支持一下。</p>]]>
    </content>
    <id>https://misterrabbit0w0.github.io/2026/06/02/2026-06-02-elegant-agent-guideline/</id>
    <link href="https://misterrabbit0w0.github.io/2026/06/02/2026-06-02-elegant-agent-guideline/"/>
    <published>2026-06-02T02:00:00.000Z</published>
    <summary>
      <![CDATA[<p>本文相关信息来源截止至 2026 年 6 月，后续可能会有变化，请以最新信息为准。</p>
<hr>
<p>这篇文章属于半扫盲性质的 agent 使用说明，小白也可放心食用~</p>
<hr>
<p>在聊如何使用之前，我们先来了解一些基础概念。</p>
<h2]]>
    </summary>
    <title>如何优雅地使用 Agent</title>
    <updated>2026-06-13T13:08:50.387Z</updated>
  </entry>
  <entry>
    <author>
      <name>织星</name>
    </author>
    <category term="工具" scheme="https://misterrabbit0w0.github.io/categories/%E5%B7%A5%E5%85%B7/"/>
    <category term="Hexo" scheme="https://misterrabbit0w0.github.io/tags/Hexo/"/>
    <category term="CSS" scheme="https://misterrabbit0w0.github.io/tags/CSS/"/>
    <category term="oklch" scheme="https://misterrabbit0w0.github.io/tags/oklch/"/>
    <content>
      <![CDATA[<p>因为没搞懂怎么挂别人主题的依赖，索性自己写了一个，取名 <strong>Hoshino</strong>，从 UI 参考稿直接开搞。</p><p>自己造主题有个意外的好处：每个像素都知道是怎么来的，出了问题不用去翻别人仓库的 issue。</p><h2 id="长什么样"><a href="#长什么样" class="headerlink" title="长什么样"></a>长什么样</h2><ul><li>米黄色打底，樱花粉做唯一主色。颜色全部用 oklch 写，亮色、暗色、跟随系统三种模式</li><li>全站字体是 <a href="https://github.com/subframe7536/maple-font">Maple Mono CN</a>——中英 2:1 的等宽字体，我一直想用它。中文子集走中文网字计划的切片 CDN，按需加载，不至于太重</li><li>文章头图不放图片，改成「星图」：拿标题做哈希种子，构建时生成一张星座连线 SVG。同一篇文章永远是同一张，每篇文章互不相同</li><li>花瓣会飘，鼠标指针是自己画的：默认箭头带一片樱花瓣，可点击的地方会变成星星魔法棒，看图时是一只金色放大镜</li></ul><h2 id="一些实现碎片"><a href="#一些实现碎片" class="headerlink" title="一些实现碎片"></a>一些实现碎片</h2><ul><li>EJS 模板 + 两个 CSS 文件，没上任何框架</li><li>暗色模式用 <code>data-color-mode</code> 切换，颜色全走 CSS 变量，星图封面也跟着一起变色</li><li>设置了 <code>prefers-reduced-motion</code> 的话，花瓣和动画会全部停掉</li></ul><p>源码就在<a href="https://github.com/misterrabbit0w0/misterrabbit0w0.github.io">博客仓库</a>的 <code>themes/hoshino/</code> 里，想扒随便扒。</p>]]>
    </content>
    <id>https://misterrabbit0w0.github.io/2026/05/31/2026-04-20-hoshino-theme/</id>
    <link href="https://misterrabbit0w0.github.io/2026/05/31/2026-04-20-hoshino-theme/"/>
    <published>2026-05-31T15:30:00.000Z</published>
    <summary>
      <![CDATA[<p>因为没搞懂怎么挂别人主题的依赖，索性自己写了一个，取名 <strong>Hoshino</strong>，从 UI 参考稿直接开搞。</p>
<p>自己造主题有个意外的好处：每个像素都知道是怎么来的，出了问题不用去翻别人仓库的 issue。</p>
<h2]]>
    </summary>
    <title>博客换装：Hoshino 主题上线</title>
    <updated>2026-06-10T04:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>织星</name>
    </author>
    <category term="译本" scheme="https://misterrabbit0w0.github.io/categories/%E8%AF%91%E6%9C%AC/"/>
    <category term="算法" scheme="https://misterrabbit0w0.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    <category term="KaTeX" scheme="https://misterrabbit0w0.github.io/tags/KaTeX/"/>
    <category term="VitePress" scheme="https://misterrabbit0w0.github.io/tags/VitePress/"/>
    <content>
      <![CDATA[<p>花了几天时间，把《The Little Book of Algorithms》翻译成了中文版，部署在 <a href="https://misterrabbit0w0.github.io/little-book-of-algorithms-zh/">misterrabbit0w0.github.io&#x2F;little-book-of-algorithms-zh</a></p><h2 id="基本情况"><a href="#基本情况" class="headerlink" title="基本情况"></a>基本情况</h2><ul><li><strong>11 章</strong>，覆盖算法基础 → 字符串 → 图论 → 动态规划 → 机器学习 &#x2F; 深度学习</li><li>中文用词尽量自然，保留原作的精炼节奏</li></ul><h2 id="几个小坑"><a href="#几个小坑" class="headerlink" title="几个小坑"></a>几个小坑</h2><p>翻译过程中踩到不少 markdown&#x2F;LaTeX 边界 bug：</p><ul><li>表格单元里的 <code>$|g|_2$</code> 会被 markdown-it 按 <code>|</code> 切列</li><li><code>$$...$$</code> 块里碰到 <code>_{subscript}</code> 会被解析成 <code>&lt;em&gt;</code></li><li>行首的 <code>+</code> 会被吃成列表项，把 <code>$$...$$</code> 劈成两半</li><li>setext heading：公式块里单独一行 <code>=</code> 会把上一行变 <code>&lt;h1&gt;</code></li></ul><p>全部在 <a href="https://github.com/misterrabbit0w0/little-book-of-algorithms-zh/blob/main/.vitepress/theme/index.ts">.vitepress&#x2F;theme&#x2F;index.ts</a> 里加了一层客户端 pass-0 解包 + regex 放宽解决了。</p><h2 id="去哪读"><a href="#去哪读" class="headerlink" title="去哪读"></a>去哪读</h2><p><a href="https://misterrabbit0w0.github.io/little-book-of-algorithms-zh/">在线版</a>（KaTeX 即时渲染，响应式布局），源码在 <a href="https://github.com/misterrabbit0w0/little-book-of-algorithms-zh">GitHub</a>。</p><p>发现翻译问题或者排版 bug，欢迎来提 issue 和 PR。</p>]]>
    </content>
    <id>https://misterrabbit0w0.github.io/2026/04/20/2026-04-20-little-book-of-algorithms-zh/</id>
    <link href="https://misterrabbit0w0.github.io/2026/04/20/2026-04-20-little-book-of-algorithms-zh/"/>
    <published>2026-04-20T02:00:00.000Z</published>
    <summary>
      <![CDATA[<p>花了几天时间，把《The Little Book of Algorithms》翻译成了中文版，部署在 <a]]>
    </summary>
    <title>《小小算法书》中文版上线</title>
    <updated>2026-06-13T13:08:50.387Z</updated>
  </entry>
</feed>
